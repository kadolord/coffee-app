import { useEffect, useState } from "react";
import { Product } from "../types";
import { filterAndSortData } from "../utils/FoodUtils";
import { data } from "../constants/data/products";

const usePriceFilter = () => {};
export default usePriceFilter;
